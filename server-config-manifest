server: (String)
plugins:
    'name':
        jar: (String)
devices: (String)           #default $RUNTIME/devices
                            {'name':
                                type: {primary,logic}       #default primary
                                device: (String)
                                mount: (String)
                                args: (struct)
                                }

#system level
rules:
    path: (String)          #default $RUNTIME/rules/system.rules
    user: (String)          #default $RUNTIME/rules/user/*.rules

log:
    system: (String)        #default $RUNTIME/log/system.log
    session: (String)       #default $RUNTIME/log/session.log